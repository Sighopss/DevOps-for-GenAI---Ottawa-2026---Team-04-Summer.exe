.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# Remote state config — holds the real bucket name and account id.
# Copy backend.tf.example to backend.tf locally; never commit it.
backend.tf
!backend.tf.example

override.tf
override.tf.json
*_override.tf
*_override.tf.json
.build/
*.tfvars
!*.tfvars.example
.terraformrc
terraform.rc
